# backend API server
# Example environment variables for the Electron desktop app
# Copy this to ".env" and edit values as needed.

BACKEND_URL=http://localhost:5000

# GitHub repo for auto-updates (overridden by hard-coded value in main.js)
UPDATE_REPO=UPDATE_REPO=ClassyKaran/KavyaShift_ExeFile
